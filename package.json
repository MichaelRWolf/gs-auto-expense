{
  "name": "gs-auto-expenses",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
      "test":       "jest --verbose",

      "build-only": "tsc",
      "build":      "npm run test && npm run build-only",

      "push-only":  "cp appsscript.json dist/ && clasp push",
      "push":       "npm run build && npm run push-only"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.2.3",
    "typescript": "^5.5.4"
  }
}
